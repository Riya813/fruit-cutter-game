import { defineConfig } from 'vite';

export default defineConfig({
  // Relative base = the same build works at any URL, including
  // GitHub Pages project paths like /fruit-cutter-game/.
  base: './',
});
